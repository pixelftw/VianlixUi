---
title: Text Reveal
description: An animated text component which renders words in a sentance or paragraph with staggered delay over each word .
---

<Tabs defaultValue="preview" className="pt-5 pb-1">
  <TabsList className="">
    <TabsTrigger value="preview">Preview</TabsTrigger>
    <TabsTrigger value="code">Code</TabsTrigger>
  </TabsList>
  <TabsContent value="preview">
    <PreviewBox>
     <TextRevealExample />
    </PreviewBox>
  </TabsContent>
  <TabsContent value="code">
  ```jsx {6} showLineNumbers
      import { TextReveal } from '@/component/ui'
      
      export default function RootLayout({ children }) {
        return (
          <TextReveal
            variant="fadeIn"
            className="text-2xl md:text-6xl font-extrabold text-white"
          >
            Text Revealing animation made Awesome 🔥
          </TextReveal>
        )}
      ````
  </TabsContent>
</Tabs>

## Usage

To use the `TextReveal` component, follow these steps:

1. **Installation**: Install the necessary package using npm, yarn or pnpm.
   ```bash
   npm install framer-motion
   ```
2. **Tailwind CSS**: Install [tailwind css](https://tailwindcss.com/docs/guides/nextjs) or you can customise the styling according to your ui library

3. **Implementation**: Copy paste the below code in your reposatory

   ```jsx
   function App() {
     const data = useSession();
     return (
       <div>
         <pre>{JSON.stringify(data, null, 2)}</pre>
       </div>
     );
   }

   export default App;
   ```

## Examples

Here are some examples of how to use the `TextReveal` component in different scenarios.

### Basic Example

```jsx
const data = useSession();
console.log(data);
```

### Advanced Example

```jsx
const data = useSession({ param: "value" });
console.log(data);
```

## Parameters

| Props        | Type     | values                               |
| ------------ | -------- | ------------------------------------ |
| `variant`    | `string` | `fadeIn` `slideIn` `flip`            |
| `paramName2` | `type`   | Another description of the parameter |

## Conclusion

The `TextReveal` Component is a versatile and performant animated resusable component built using framer motion. It can be easily customized based on your project requirement
